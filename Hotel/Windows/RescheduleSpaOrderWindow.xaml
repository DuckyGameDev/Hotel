<Window x:Class="Hotel.Windows.RescheduleSpaOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Перенос SPA-процедуры" 
        Height="300" 
        Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#FFF0F5FF">
        <Border Background="White" 
                CornerRadius="5"
                Margin="20"
                Padding="20">
            <StackPanel>
                <TextBlock Text="Перенос процедуры" 
                          FontSize="18" 
                          FontWeight="Bold"
                          Margin="0 0 0 20"/>

                <TextBlock Text="{Binding Order.SpaService.ServiceName}" 
                          FontWeight="Bold"
                          Margin="0 0 0 10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Дата -->
                    <TextBlock Text="Новая дата:" 
                              Grid.Row="0" Grid.Column="0" 
                              Margin="0 0 10 10"/>
                    <DatePicker Grid.Row="0" Grid.Column="1" 
                               SelectedDate="{Binding NewDate}"
                               Margin="0 0 0 10"/>

                    <!-- Время -->
                    <TextBlock Text="Новое время:" 
                              Grid.Row="1" Grid.Column="0" 
                              Margin="0 0 10 10"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" 
                             ItemsSource="{Binding AvailableTimes}"
                             SelectedValue="{Binding NewTime}"
                             DisplayMemberPath="Value"
                             SelectedValuePath="Key"
                             Margin="0 0 0 20"/>
                </Grid>

                <!-- Кнопки -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="Сохранить" 
                            Width="120" 
                            Margin="0 0 10 0"
                            Click="SaveButton_Click"
                            Background="#FF4A6FAE"
                            Foreground="White"/>
                    <Button Content="Отмена" 
                            Width="120"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>