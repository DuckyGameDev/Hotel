<Window x:Class="Hotel.Windows.EmployeeManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.Windows"
        mc:Ignorable="d"
        Title="Управление сотрудниками" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#FFF0F5FF">
        <DockPanel>
            <!-- Шапка -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" 
                       Background="#FF4A6FAE" Height="50">
                <Button Content="Назад" 
                        Width="100" 
                        Background="Transparent" 
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Margin="10 0 0 0"
                        Click="BackButton_Click"/>
                <TextBlock Text="Управление сотрудниками" 
                          Foreground="White"
                          VerticalAlignment="Center"
                          FontSize="16"
                          FontWeight="Bold"
                          Margin="20 0 0 0"/>
            </StackPanel>

            <!-- Основное содержимое -->
            <Grid DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Верхняя часть - информация о сотруднике -->
                <GroupBox Grid.Row="0" Header="Информация о сотруднике" Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="ФИО:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="FullNameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Должность:" Grid.Row="0" Grid.Column="2" Margin="5"/>
                        <TextBox x:Name="PositionTextBox" Grid.Row="0" Grid.Column="3" Margin="5"/>

                        <TextBlock Text="Телефон:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="PhoneTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Email:" Grid.Row="1" Grid.Column="2" Margin="5"/>
                        <TextBox x:Name="EmailTextBox" Grid.Row="1" Grid.Column="3" Margin="5"/>

                        <TextBlock Text="Дата приема:" Grid.Row="2" Grid.Column="0" Margin="5"/>
                        <DatePicker x:Name="HireDatePicker" Grid.Row="2" Grid.Column="1" Margin="5"/>

                        <StackPanel Grid.Row="4" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
                            <Button x:Name="AddButton" Content="Добавить" Width="100" Margin="5" Click="AddButton_Click"/>
                            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Margin="5" Click="SaveButton_Click"/>
                            <Button x:Name="CancelButton" Content="Отмена" Width="100" Margin="5" Click="CancelButton_Click"/>
                            <Button x:Name="DeleteButton" Content="Удалить" Width="100" Margin="5" Background="#FFD32F2F" Foreground="White" Click="DeleteButton_Click"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Нижняя часть - список сотрудников -->
                <GroupBox Grid.Row="1" Header="Список сотрудников" Margin="10">
                    <DockPanel>
                        <TextBox x:Name="SearchTextBox" DockPanel.Dock="Top" Margin="0 0 0 10" 
                                 TextChanged="SearchTextBox_TextChanged" />
                        <!--PlaceholderText="Поиск..."/>-->

                        <DataGrid x:Name="EmployeesDataGrid" 
                                 AutoGenerateColumns="False"
                                 SelectionMode="Single"
                                 SelectionChanged="EmployeesDataGrid_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="Auto" IsReadOnly="True"/>
                                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                                <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="150"/>
                                <DataGridTextColumn Header="Телефон" Binding="{Binding ContactPhone}" Width="120"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding ContactEmail}" Width="150"/>
                                <DataGridTextColumn Header="Дата приема" Binding="{Binding HireDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </GroupBox>
            </Grid>
        </DockPanel>
    </Grid>
</Window>