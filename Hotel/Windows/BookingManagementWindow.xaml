<Window x:Class="Hotel.Windows.BookingManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.Windows"
        mc:Ignorable="d"
        Title="Управление бронированиями" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#FFF0F5FF">
        <DockPanel>
            <!-- Шапка -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" 
                       Background="#FF4A6FAE" Height="50">
                <Button Content="Назад" 
                        Width="100" 
                        Background="Transparent" 
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Margin="10 0 0 0"
                        Click="BackButton_Click"/>
                <TextBlock Text="Управление бронированиями" 
                          Foreground="White"
                          VerticalAlignment="Center"
                          FontSize="16"
                          FontWeight="Bold"
                          Margin="20 0 0 0"/>
            </StackPanel>

            <!-- Основное содержимое -->
            <Grid DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Верхняя часть - информация о бронировании -->
                <GroupBox Grid.Row="0" Header="Информация о бронировании" Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Гость:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <ComboBox x:Name="GuestComboBox" Grid.Row="0" Grid.Column="1" Margin="5"
                                 DisplayMemberPath="FullName"/>

                        <TextBlock Text="Номер:" Grid.Row="0" Grid.Column="2" Margin="5"/>
                        <ComboBox x:Name="RoomComboBox" Grid.Row="0" Grid.Column="3" Margin="5"
                                 DisplayMemberPath="RoomNumber"/>

                        <TextBlock Text="Дата заезда:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                        <DatePicker x:Name="CheckInDatePicker" Grid.Row="1" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Дата выезда:" Grid.Row="1" Grid.Column="2" Margin="5"/>
                        <DatePicker x:Name="CheckOutDatePicker" Grid.Row="1" Grid.Column="3" Margin="5"/>

                        <StackPanel Grid.Row="3" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
                            <Button x:Name="AddButton" Content="Добавить" Width="100" Margin="5" Click="AddButton_Click"/>
                            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Margin="5" Click="SaveButton_Click"/>
                            <Button x:Name="CancelButton" Content="Отмена" Width="100" Margin="5" Click="CancelButton_Click"/>
                            <Button x:Name="DeleteButton" Content="Удалить" Width="100" Margin="5" Background="#FFD32F2F" Foreground="White" Click="DeleteButton_Click"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Нижняя часть - список бронирований -->
                <GroupBox Grid.Row="1" Header="Список бронирований" Margin="10">
                    <DockPanel>
                        <TextBox x:Name="SearchTextBox" DockPanel.Dock="Top" Margin="0 0 0 10" 
                                 TextChanged="SearchTextBox_TextChanged"/>

                        <DataGrid x:Name="BookingsDataGrid" 
                                 AutoGenerateColumns="False"
                                 SelectionMode="Single"
                                 SelectionChanged="BookingsDataGrid_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding BookingId}" Width="Auto" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Гость" Binding="{Binding Guest.FullName}" Width="*"/>
                                <DataGridTextColumn Header="Номер" Binding="{Binding Room.RoomNumber}" Width="100"/>
                                <DataGridTextColumn Header="Категория" Binding="{Binding Room.Category.CategoryName}" Width="120"/>
                                <DataGridTextColumn Header="Заезд" Binding="{Binding CheckInDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                                <DataGridTextColumn Header="Выезд" Binding="{Binding CheckOutDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </GroupBox>
            </Grid>
        </DockPanel>
    </Grid>
</Window>